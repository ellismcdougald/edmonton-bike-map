name: "CI/CD Backend"

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    container:
      image: williamyeh/ansible:alpine3

    env:
      IMAGE_NAME: ghcr.io/ellismcdougald/edmonton-bike-backend
      IMAGE_TAG: latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.24"

      - name: Run tests
        run: go test ./...
        working-directory: backend

      - name: Build Docker image
        run: docker build --progress=plain -f backend/Dockerfile -t $IMAGE_NAME:$IMAGE_TAG backend/

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Push Docker image
        run: docker push $IMAGE_NAME:$IMAGE_TAG

      - name: Run backend playbook
        run: ansible-playbook backend.yml -i inventory.ini
        env:
          ANSIBLE_HOST_KEY_CHECKING: "False"
          SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_KEY }}
